import Vue from 'vue'
import { Icon, Input, Pagination, Select, Option } from 'element-ui'

Vue.prototype.$ELEMENT = { size: 'medium', zIndex: 3000 }

Vue.use(Icon)
Vue.use(Input)
Vue.use(Pagination)
Vue.use(Select)
Vue.use(Option)
